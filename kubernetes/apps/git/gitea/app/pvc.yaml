apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea
  namespace: git
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-rbd
